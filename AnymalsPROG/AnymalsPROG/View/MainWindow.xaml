<Window x:Class="AnymalsPROG.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnymalsPROG.View"
        xmlns:dc="clr-namespace:AnymalsPROG.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <dc:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <DataGrid AutoGenerateColumns="false" ItemsSource="{Binding AnimalsList}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="id" Binding="{Binding Path=id}"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Path=name}"/>
                <DataGridTextColumn Header="Цвет" Binding="{Binding Path=color}"/>
                <DataGridTextColumn Header="Дата рождания" Binding="{Binding Path=birthday}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
